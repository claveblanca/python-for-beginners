"""
Chapter 9 — shared settings.

Keeps configurable values out of the main scripts so they are easy to
find and change in one place.
"""

APP_NAME = "Python Beginner Book — Chapter 9"
DEFAULT_AUDIENCE = "World"
GREETING_PREFIX = "Hello"

SAMPLE_PACKAGES = [
    "requests",
    "rich",
    "python-dateutil",
]

PYPI_SEARCH_URL = "https://pypi.org/pypi/{package}/json"
